apiVersion: v1
kind: Service
metadata:
  name: ai-kickstarts-app
  labels:
    app: ai-kickstarts-app
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: ai-kickstarts-app