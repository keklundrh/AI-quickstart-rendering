apiVersion: v1
kind: Secret
metadata:
  name: github-token
  labels:
    app: ai-kickstarts-app
type: Opaque
stringData:
  token: ${GH_TOKEN}  # This will be replaced with the actual token during deployment